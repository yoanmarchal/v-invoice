[build]
  ignore = "git diff --quiet HEAD^ HEAD packages/lib/"
  # run install at repo root then build documentation workspace
  command = "npm ci && npm --prefix packages/documentation run docs:build"
  publish = "packages/documentation/docs/.vitepress/dist"
